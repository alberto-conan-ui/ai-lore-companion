/**
 * Argument, result and payload types of the channels of the machine check.
 * Empty until phase M3.6 fills it. Plain data only; types from core are
 * imported with `import type`. `shared/ipc.ts` already re-exports this file.
 */

export {};
