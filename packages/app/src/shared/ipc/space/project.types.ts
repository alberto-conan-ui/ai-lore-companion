/**
 * Argument, result and payload types of the channels of the Space's GitHub
 * Project. Empty until phase M7.1 fills it. Plain data only; types from core
 * are imported with `import type`. `shared/ipc.ts` already re-exports this
 * file.
 */

export {};
