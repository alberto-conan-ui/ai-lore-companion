/**
 * Argument, result and payload types of the channels of the Files window.
 * Plain data only; types from core are imported with `import type`.
 * `shared/ipc.ts` already re-exports this file.
 *
 * M5.6's search keeps its types in `./root-search.types.ts`, re-exported here.
 */

export * from './root-search.types.js';
export * from './root-file-edit.types.js';
export * from './ui.types.js';
