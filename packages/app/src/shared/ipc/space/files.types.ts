/**
 * Argument, result and payload types of the channels of the Files window.
 * Empty until the M5 phases fill it. Plain data only; types from core are
 * imported with `import type`. `shared/ipc.ts` already re-exports this file.
 */

export {};
