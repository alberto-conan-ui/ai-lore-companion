/**
 * Argument, result and payload types of the channels of roots. Empty until
 * phase M5.1 fills it. Plain data only; types from core are imported with
 * `import type`. `shared/ipc.ts` already re-exports this file.
 */

export {};
