/**
 * Argument, result and payload types of the channels of the two dialogs.
 * Empty until phase M4.5 fills it. Plain data only; types from core are
 * imported with `import type`. `shared/ipc.ts` already re-exports this file.
 */

export {};
