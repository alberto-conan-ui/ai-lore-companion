/**
 * Argument, result and payload types of the channels of creating a Space.
 * Empty until phase M3.7 fills it. Plain data only; types from core are
 * imported with `import type`. `shared/ipc.ts` already re-exports this file.
 */

export {};
